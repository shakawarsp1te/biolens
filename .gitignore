# --- app (Expo) ---
app/node_modules/
app/.expo/
app/dist/
app/.env.local
app/*.log

# --- api (FastAPI) ---
api/.venv/
api/__pycache__/
api/**/__pycache__/
api/.pytest_cache/
api/.ruff_cache/
api/.env
# Interim local user store (api/app/services/user_store.py) — real password
# hashes and email addresses live here today, ahead of Supabase Auth being
# provisioned. Never committed, same as api/.env.
api/db/*.sqlite3

# --- general ---
.DS_Store
*.pem
.claude/
CLAUDE.md
AGENTS.md
.Rhistory
